import { Activity, ArrowDownToLine, ArrowUpFromLine, Boxes, CircleAlert } from 'lucide-react'
import { coldRooms } from '@/features/dashboard/services/dashboard.mock'
import { ColdRoomCard } from '@/features/dashboard/components/ColdRoomCard'

const overview = [{ label: 'Total Capacity', value: '3,000', detail: 'pallet locations', icon: Boxes, tone: 'bg-sky-500/15 text-sky-400' }, { label: 'Occupancy', value: '78.4%', detail: 'across all cold rooms', icon: Activity, tone: 'bg-orange-500/15 text-orange-400' }, { label: 'Inbound Today', value: '84', detail: 'pallets received', icon: ArrowDownToLine, tone: 'bg-emerald-500/15 text-emerald-400' }, { label: 'Outbound Today', value: '103', detail: 'pallets dispatched', icon: ArrowUpFromLine, tone: 'bg-violet-500/15 text-violet-400' }]
export function DashboardPage() { return <section><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Operations overview</p><h1 className="page-title">Warehouse Digital Twin</h1><p className="mt-2 text-sm text-slate-400">Live view of cold storage capacity and daily movements.</p></div><div className="flex items-center gap-2 rounded-lg border border-amber-500/40 bg-amber-500/15 px-3 py-2 text-xs text-amber-200"><CircleAlert className="size-4" />2 rooms need attention</div></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{overview.map(({ label, value, detail, icon: Icon, tone }) => <div key={label} className="rounded-xl border border-slate-700 bg-slate-800 p-5 shadow-sm"><div className={`inline-flex size-9 items-center justify-center rounded-lg ${tone}`}><Icon className="size-5" /></div><p className="mt-4 text-sm font-medium text-slate-400">{label}</p><p className="mt-1 text-2xl font-semibold tracking-tight text-slate-50">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></div>)}</div><div className="mt-10 flex items-center justify-between"><div><h2 className="text-lg font-semibold text-slate-50">Cold Room Status</h2><p className="mt-1 text-sm text-slate-400">Select a room to open its digital twin.</p></div><div className="hidden items-center gap-3 text-xs text-slate-400 md:flex"><Legend color="bg-emerald-500" label="Occupied" /><Legend color="bg-slate-600" label="Empty" /><Legend color="bg-amber-400" label="Reserved" /><Legend color="bg-red-500" label="Blocked" /></div></div><div className="mt-5 grid gap-5 xl:grid-cols-2">{coldRooms.map((room) => <ColdRoomCard key={room.id} room={room} />)}</div></section> }
function Legend({ color, label }: { color: string; label: string }) { return <span className="flex items-center gap-1.5"><i className={`size-2.5 rounded-sm ${color}`} />{label}</span> }
